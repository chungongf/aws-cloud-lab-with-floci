# Applications

Application-specific Kubernetes manifests (Deployments, Services, Ingress, etc.) go here — one subdirectory per app. Apply with:

```bash
kubectl apply -f kubernetes/applications/<app-name>/
```
