# EC2 Example

Minimal, standalone example for launching a single EC2 instance. Intended to be run independently of the main `terraform/` stack for learning purposes.

```bash
terraform init
terraform apply
```
